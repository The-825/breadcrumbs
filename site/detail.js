const esc=(v="")=>String(v).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));const qs=new URLSearchParams(location.search);const type=qs.get("type"),id=qs.get("id");
const pills=values=>values.map(x=>`<span class="pill">${esc(x)}</span>`).join("");
function research(item,all,repos){const related=all.filter(x=>x.id!==item.id&&x.claims.some(c=>item.claims.includes(c))).slice(0,8);const linked=repos.filter(x=>x.claims.some(c=>item.claims.includes(c))).slice(0,8);return `<p class="eyebrow">Research profile</p><h1>${esc(item.title)}</h1><p class="lede">${esc(item.finding)}</p><div class="profile"><div class="meter"><small>Evidence order</small><strong>${item.evidenceOrder} / 100</strong></div><div class="meter"><small>Directness</small><strong>${esc(item.directness)}</strong></div><div class="meter"><small>Horizon</small><strong>${esc(item.horizon)}</strong></div><div class="meter"><small>Evidence family</small><strong>${esc(item.family)}</strong></div></div><section class="detail"><h2>What it changes</h2><p>${esc(item.impact)}</p><h3>Boundary and next question</h3><p>${esc(item.boundary)}</p><h3>Visible risks</h3><p>${esc(item.flags)}</p><h3>Linked claims</h3>${pills(item.claims)}<p><a class="button" href="${esc(item.url)}">Open original source</a></p></section><section class="section related"><div class="card"><h2>Related research</h2>${related.map(x=>`<a href="detail.html?type=research&id=${x.id}">${esc(x.title)}</a>`).join("")||"<p>No related records.</p>"}</div><div class="card"><h2>Related repositories</h2>${linked.map(x=>`<a href="detail.html?type=repositories&id=${x.id}">${esc(x.repository)}</a>`).join("")||"<p>No related records.</p>"}</div></section>`}
function repository(item,all,researchItems){const related=all.filter(x=>x.id!==item.id&&(x.selection_aspect===item.selection_aspect||x.claims.some(c=>item.claims.includes(c)))).slice(0,8);const linked=researchItems.filter(x=>x.claims.some(c=>item.claims.includes(c))).slice(0,8);const visible=Object.entries(item.mechanisms).filter(([,v])=>v==="V").map(([k])=>k.replaceAll("_"," "));const partial=Object.entries(item.mechanisms).filter(([,v])=>v==="P").map(([k])=>k.replaceAll("_"," "));return `<p class="eyebrow">Repository profile</p><h1>${esc(item.repository)}</h1><p class="lede">${esc(item.evidence_note)}</p><div class="profile"><div class="meter"><small>Popularity order</small><strong>${item.popularityOrder} / 100</strong></div><div class="meter"><small>Stars observed</small><strong>${Number(item.stars_observed).toLocaleString()}</strong></div><div class="meter"><small>Evidence depth</small><strong>${esc(item.evidence_depth)}</strong></div><div class="meter"><small>Lifecycle</small><strong>${esc(item.lifecycle)}</strong></div></div><section class="detail"><h2>Mechanism profile</h2><h3>Visible</h3>${pills(visible)}<h3>Partial</h3>${pills(partial)}<h3>Linked claims</h3>${pills(item.claims)}<p class="note">Popularity is the audience signal. Evidence depth and mechanism observations are the review signals. They remain separate.</p><p><a class="button" href="${esc(item.url)}">Open GitHub repository</a></p></section><section class="section related"><div class="card"><h2>Related repositories</h2>${related.map(x=>`<a href="detail.html?type=repositories&id=${x.id}">${esc(x.repository)}</a>`).join("")}</div><div class="card"><h2>Related research</h2>${linked.map(x=>`<a href="detail.html?type=research&id=${x.id}">${esc(x.title)}</a>`).join("")||"<p>No related records.</p>"}</div></section>`}
async function init(){if(!["research","repositories"].includes(type)||!id)throw Error();const [current,researchItems,repos]=await Promise.all([fetch(`data/${type}.json`).then(r=>r.json()),fetch("data/research.json").then(r=>r.json()),fetch("data/repositories.json").then(r=>r.json())]);const item=current.find(x=>x.id===id);if(!item)throw Error();document.title=`${item.title||item.repository} | Breadcrumbs`;document.querySelector("main").innerHTML=type==="research"?research(item,researchItems,repos):repository(item,repos,researchItems)}
init().catch(()=>{document.querySelector("main").innerHTML='<p class="eyebrow">Not found</p><h1>This breadcrumb is missing.</h1><p><a href="index.html">Return to the Trail Map</a></p>'});
